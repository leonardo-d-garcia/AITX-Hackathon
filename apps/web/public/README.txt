Place titan_avenger.glb here (copied from fixtures/c/titan_avenger_cad/meshes/) so Vite serves /titan_avenger.glb.

The mesh is gitignored at the repo root (*.glb). Do not commit it.

Rebuild via:

  .venv\Scripts\python.exe tools/convert_titan.py
